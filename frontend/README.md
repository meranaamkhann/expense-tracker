# SpendWise frontend

A complete Next.js 16 frontend for the expense tracker. It uses browser local storage for the current demo data because the supplied backend has no authentication, category, or persistence APIs yet.

## Run

```bash
npm install
npm run dev
```

Open `http://localhost:3000`. Create an account or use the demo account shown on the login page.
