"use client";
import { FormEvent, useState } from "react";
import { signOut } from "@/lib/store";
import { useWallet } from "@/lib/use-wallet";
import { useRouter } from "next/navigation";

export default function ProfilePage() { const {user}=useWallet(); const router=useRouter(); const [message,setMessage]=useState(""); const save=(e:FormEvent)=>{e.preventDefault();setMessage("Your personal details are stored locally in this demo. A connected backend can make these changes durable across devices.");}; const leave=()=>{signOut();router.push("/");}; return <div className="mx-auto max-w-3xl"><p className="eyebrow">Your space</p><h1 className="mt-2 text-4xl font-semibold tracking-tight">Keep it feeling like yours.</h1><section className="card mt-8 p-6"><h2 className="text-xl font-semibold">Personal details</h2><form onSubmit={save} className="mt-6 grid gap-4 sm:grid-cols-2"><label className="text-sm font-semibold">Name<input defaultValue={user?.name} className="mt-2 w-full rounded-xl border border-[#d8d2c6] bg-white px-3 py-2.5" /></label><label className="text-sm font-semibold">Email<input defaultValue={user?.email} className="mt-2 w-full rounded-xl border border-[#d8d2c6] bg-white px-3 py-2.5" /></label><button className="rounded-xl bg-[#3e6b50] px-5 py-3 font-bold text-white sm:col-span-2">Save details</button></form>{message&&<p className="mt-4 rounded-xl bg-[#dce9dd] p-3 text-sm text-[#28533a]">{message}</p>}</section><section className="mt-6 rounded-[22px] border border-[#f0cfc0] bg-[#fff0e9] p-6"><h2 className="text-xl font-semibold text-[#763e2b]">Leave this device</h2><p className="mt-2 text-sm leading-6 text-[#875746]">This signs you out here. Your browser&apos;s demo data will remain until you clear it.</p><button onClick={leave} className="mt-5 rounded-xl border border-[#c96c4b] px-5 py-2.5 font-bold text-[#9d482f]">Log out</button></section></div>; }
